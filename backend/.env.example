# TaylorDash Backend Environment Configuration

# Application Environment
ENVIRONMENT=development

# Database Configuration
DATABASE_URL=postgresql://taylordash:taylordash@postgres:5432/taylordash

# MQTT Configuration
MQTT_HOST=mosquitto
MQTT_PORT=1883
MQTT_USERNAME=taylordash
MQTT_PASSWORD=taylordash

# OIDC/Keycloak Configuration
KEYCLOAK_URL=https://tracker.local/kc
KEYCLOAK_REALM=taylordash
KEYCLOAK_CLIENT_ID=taylordash-frontend
OIDC_ISSUER=https://tracker.local/kc/realms/taylordash
OIDC_AUDIENCE=taylordash-frontend

# Authentication Development Settings
# Set to "true" to skip JWT signature verification (development only)
SKIP_JWT_VERIFICATION=false

# Logging Configuration
LOG_LEVEL=INFO

# OpenTelemetry Configuration (optional)
OTEL_SERVICE_NAME=taylordash-backend
OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4317

# CORS Configuration (automatically handled based on ENVIRONMENT)
# Production origins: https://tracker.local
# Development origins: http://localhost:5173, http://127.0.0.1:5173