# TaylorDash Environment Variables

# API Security
API_KEY=taylordash-dev-key

# Database Configuration
POSTGRES_PASSWORD=taylordash

# MinIO Configuration
MINIO_ROOT_USER=taylordash
MINIO_ROOT_PASSWORD=taylordash123

# Grafana Configuration
GRAFANA_PASSWORD=admin

# TimescaleDB Configuration (optional)
TIMESCALE_PASSWORD=taylordash

# Frontend (Dev) — Remote-safe API origin
# Example: http://127.0.0.1:8000 or https://taylordash.local
VITE_API_ORIGIN=

# Frontend Plugins (optional dev overrides)
# VITE_PLUGIN_ORIGIN can set a base for plugin entry points
VITE_PLUGIN_ORIGIN=
VITE_PLUGIN_MCP_MANAGER=
VITE_PLUGIN_MIDNIGHT_HUD=
VITE_PLUGIN_PROJECTS_MANAGER=
